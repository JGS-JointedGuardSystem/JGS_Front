export interface ContainerProps {
  bgImage: string;
}

export interface LoginInputDataType {
  title: string;
  name: string;
  placeholder: string;
  type: string;
}

export interface LoginInputType {
  id: string;
  password: string;
}
